interface:
  display_name: "Codex Game Studio Default"
  short_description: "Game workflow with default animal UI, assets, and motion"
  default_prompt: "Use $codex-game-studio-default to deliver this game task with the default UI, animal asset, and procedural-motion workflow."

policy:
  allow_implicit_invocation: true
